"""Autoresearch-side thin HTTP clients for the evolution-judge service.

Prompts and agent logic live on the judge-service (see judges/ at the repo
root and Plan A Phase 0c). This package is the client side only; it knows
only how to build HTTP requests and parse responses.
"""
