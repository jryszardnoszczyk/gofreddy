# linkedin_engine templates — placeholder
This directory is referenced by LaneSpec.path_prefixes (programs/templates/linkedin_engine).
v1 of the lane has no per-lane templates; the agent's prompt+tools cover the shape.
Templates land here if a future generation introduces lane-specific scaffolds.
