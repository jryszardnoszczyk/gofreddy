# LinkedIn sources — comprehensive research lane configuration for linkedin_engine.
#
# Hand-curated 2026-05-08 to scaffold L1 LinkedIn pull cadence (per master plan
# v13 §3.4 + §7.3). Frozen for v1+v2; JR-only hot-edit channel after that.
#
# Two surfaces:
#   1. linkedin_keywords  — daily 06:35 keyword search (cost-cap --max-cu 50)
#   2. linkedin_users     — weekly Sun 07:00 per-creator pull (cost-cap --max-cu 200)
#
# Both populate `linkedin_posts` table; `xeng top-linkedin` ranks by the
# decay-weighted engagement formula (Round-7 Gap B):
#     score = (reactions×1 + comments×3 + shares×5) × exp(-days/14)
#
# OPERATOR TASK (gate #5 in pre-L0): populate this file with ~50 creators +
# ~20 keywords from JR's niche before the linkedin-pull-* plists go live.
# The lists below are scaffold placeholders — replace before activation.

linkedin_keywords:
  # Niche keywords (JR's: AI-native marketing agency / AI / coding agents)
  # ~20 hand-curated queries — replace with the operator's chosen set.
  - "AI marketing agency"
  - "Claude Code workflow"
  - "agentic marketing"
  - "compound engineering"
  - "AI agent harness"
  - "MCP servers marketing"
  - "AI go-to-market"
  - "agency operations AI"
  - "B2B marketing AI"
  - "growth engineering AI"

linkedin_users:
  # Profile URLs (one per line, full https://linkedin.com/in/<handle>) —
  # ~50 hand-curated creators — replace with the operator's chosen set.
  # Targets: B2B operators, agency leads, marketing thought leaders that
  # JR's audience listens to. NOT the same shape as X — LinkedIn audiences
  # respond to story-led / case-study / lesson-led posts, not contrarian X
  # hot-takes.
  # - https://linkedin.com/in/<handle1>
  # - https://linkedin.com/in/<handle2>
  # ...
