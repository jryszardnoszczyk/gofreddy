{
  "name": "gofreddy-workers",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers + R2 backing for gofreddy.ai marketing audit funnel.",
  "scripts": {
    "deploy:intake": "cd intake && wrangler deploy",
    "deploy:stripe": "cd stripe-webhook && wrangler deploy",
    "deploy:scan": "cd scan-hosting && wrangler deploy",
    "deploy:audit": "cd audit-hosting && wrangler deploy",
    "deploy:all": "npm run deploy:intake && npm run deploy:stripe && npm run deploy:scan && npm run deploy:audit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  }
}
