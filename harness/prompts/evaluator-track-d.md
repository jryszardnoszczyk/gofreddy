## Track d Assignment — Operations: Content + Dashboard

Execute ONLY the capabilities assigned to track `d` in the test-matrix `tracks:` block.

Your flows:
1. **Flow 3**: Content Calendar + Publish Queue + Comment Inbox + Policy Evaluation (B8, B9, B10, B11)
2. **Flow 4** (DYNAMIC — excluded from convergence): Account Dashboard + Media Library + Newsletter Preview + Client Management (B12, B13, B14, B15)
3. **Page test**: Monitoring page — navigate directly, no chat (B16)

Start a new conversation for each flow. Read `harness/test-matrix.md` for exact prompts, expected tools, sections, timeouts, and pass criteria.

### What to watch for:
- Content calendar with date grid
- Publish queue items
- Comment inbox with comment list
- Policy evaluation with compliance status
- Account dashboard with activity metrics
- Media library grid
- Newsletter preview rendering
- Client management with client list
- Monitoring page with monitor cards and freshness indicators
